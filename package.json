{
  "name": "distribucache-memory-store",
  "version": "4.0.0",
  "description": "RAM-backed Distribucache Store",
  "keywords": [
    "distribucache",
    "cache"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/dowjones/distribucache-memory-store.git"
  },
  "author": "nemtsov@gmail.com",
  "main": "./lib",
  "scripts": {
    "test": "sh -c 'npm run test-cover && istanbul check-coverage --statements 95 --functions 95 --branches 95 --lines 95'",
    "test-cover": "istanbul cover _mocha -- -r should --recursive",
    "test-watch": "mocha -r should --recursive -w -R min"
  },
  "devDependencies": {
    "async": "^0.9.0",
    "istanbul": "^0.3.8",
    "mocha": "^2.2.1",
    "proxyquire": "^1.4.0",
    "require-directory": "^2.1.0",
    "should": "^5.2.0",
    "sinon": "^1.14.1"
  },
  "dependencies": {
    "lease": "^1.0.0"
  }
}
